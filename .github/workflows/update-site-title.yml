name: Update site title

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  update-title:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update SEO titles
        shell: python
        run: |
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          old = 'Burak Akgül | Electronics Engineer'
          new = 'Burak Akgül | Elektronik Mühendisi & Proje Yöneticisi'
          s = s.replace(old, new)
          p.write_text(s, encoding='utf-8')
      - name: Commit change if needed
        run: |
          if git diff --quiet -- index.html; then
            echo "No change needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Update site title to Turkish"
          git push
